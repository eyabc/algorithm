function sol(num) {
    let arr = (num+"").split("")
    let arr = (num+"").length
    let b = num
    b = a % 2 ? '1'+"0".repeat(a) : num
    let n = Math.log(b.length) / Math.log(2);

    b.charAt()

    return parseInt(b)
}
console.log(sol(211))